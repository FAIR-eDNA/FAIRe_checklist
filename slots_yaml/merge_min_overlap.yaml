merge_min_overlap:
  description: Minimum overlaps required to merge paired end reads. Unit = base pair
  range: string
  annotations:
    section: Bioinformatics
    requirement_level: Recommended
    unit: base pair
    example: '12'
